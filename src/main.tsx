import React from 'react'
import ReactDOM from 'react-dom/client'
import { MovieChallenge } from './MovieChallenge'
import './styles.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <MovieChallenge />
  </React.StrictMode>
)
